Circular avatar — shows an image when `src` is set, otherwise initials on a brand gradient.

```jsx
<Avatar name="Charlotte N." variant="spark" size={48} />
<Avatar name="Liam" src="/photo.jpg" />
```

Props: `name` (initials + tooltip), `src`, `size` (px), `variant` (`spark` / `sunrise` / `night`).
