# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc

# Env / secrets
.env
.env.*
!.env.example

# Build output
dist/
build/
.next/
.netlify/
public-dist/
.public-dist-*/

# Local graph runtime state and generated projection
.state/
PROJECT_STATE.md

# OS / editor
.DS_Store
*.log
.playwright-cli/
.idea/
.vscode/

# Redundant archive (unzipped folder is source of truth)
Code the Future — Design System.zip

# Local Claude Code config, tooling, and installed skills (not repo assets;
# the design system is canonical at "Code the Future — Design System/")
.claude/

# Local Supabase client config (anon key) + CLI env
supabase/.env
