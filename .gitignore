# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc

# Env / secrets
.env
.env.*
!.env.example

# Build output
dist/
build/
.next/
.netlify/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Redundant archive (unzipped folder is source of truth)
Code the Future — Design System.zip

# Local Claude Code config, tooling, and installed skills (not repo assets;
# the design system is canonical at "Code the Future — Design System/")
.claude/
