# Copy this file to ".env" (same folder) and paste your real key.
# ".env" is gitignored — it will NOT be committed. Never put a real key in
# .env.example, in the HTML/JS, or in chat.

OPENAI_API_KEY=sk-your-key-here

# Optional — defaults to gpt-4o-mini (cheap + capable for this lesson).
# OPENAI_MODEL=gpt-4o-mini
